# AgriNova Official Website — Production Web Build

This is the production website source for AgriNova (AGN).

Important:
- The site does NOT contain a fabricated Solana mint address.
- The token is intentionally represented as pre-launch until the real deployment is completed.
- Replace the pre-launch contract section only after the real mint address and transaction signatures are verified.
- Wallet connection currently detects Phantom-compatible Solana providers. Transaction/presale actions are intentionally not enabled before the real contract and sale infrastructure exist.
- Deploy this static folder directly to Vercel.
